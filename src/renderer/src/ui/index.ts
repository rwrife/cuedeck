/**
 * Shared CueDeck Studio renderer primitives (#32).
 *
 * Import UI building blocks from this barrel so surfaces depend on the design
 * system, not ad-hoc markup: `import { Button, IconButton, Icon } from '../ui'`.
 */
export { Icon, type IconProps } from './Icon'
export { Button, type ButtonProps } from './Button'
export { IconButton, type IconButtonProps } from './IconButton'
